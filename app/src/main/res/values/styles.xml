<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>


    </style>

    <style name="MyDialog" parent="@android:style/Theme.Dialog">
        <item name="tint">@color/colorAccent</item>
        <item name="android:alertDialogStyle">@style/Widget.AppCompat.Button.Borderless</item>
        <item name="android:background">@color/transparentcolor</item>
        <item name="android:borderlessButtonStyle">@color/transparentcolor</item>
    </style>

    <style name="CustomAlertDialogStyle">
        <item name="background">@color/transparentcolor</item>
       <item name="android:gravity">center</item>
        <item name="android:selectableItemBackgroundBorderless">@color/transparentcolor</item>

    </style>
</resources>